# mtp - Render ready

This repository deploys mtp via Render. Add an Environment Variable SECRET (32 hex chars) and deploy as a Web Service.

Build Command:
    pip install -r requirements.txt

Start Command:
    bash start.sh
